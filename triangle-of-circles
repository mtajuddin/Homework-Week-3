int border = 20;
void setup() {
size(600, 600);
background(0);
}

void draw() {
  stroke(255);
  
  int y = 20;
  
  while (y < 300) {
    int x = y;
    while (x <= height - border - y) {
      ellipse (x, y, 20, 20);
      x += 30;
    }
    y += 30;
    }
}
